import React from "react";
import ReactDOM from "react-dom/client";
import DirFusion from "./DirFusion";

ReactDOM.createRoot(document.getElementById("root") as HTMLElement).render(
  <React.StrictMode>
    <DirFusion />
  </React.StrictMode>,
);
